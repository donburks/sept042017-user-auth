<% if (errors.length) { %>
  <% errors.forEach((error) => { %>
    <p class="flash error">Error: <%= error %></p>
  <% }) %>
<% } %>
<% if (info.length) { %>
  <% info.forEach((message) => { %>
    <p class="flash info">Info: <%= message %></p>
  <% }) %>
<% } %>
